# Architecture: {product-name}

> Generated by PM Copilot on {date}

## Application Type

{APP_TYPE} — {brief description based on product context one-liner}

## Architecture Pattern

{ARCH_PATTERN} — {rationale from discussion}

## Tech Stack

| Layer | Technology | Notes |
|-------|-----------|-------|
| Language | {from DETECTED_STACK or Q&A} | |
| Backend Framework | {BACKEND_STACK} | |
| Frontend Framework | {FRONTEND_STACK} | |
| Database | {from DATA_LAYER} | |
| ORM / Data Layer | {from DATA_LAYER} | |
| UI Library | {UI_LIBRARY} | |
| Test Framework | {from DETECTED_STACK} | |
| Package Manager | {from DETECTED_STACK} | |
| Deployment | {DEPLOYMENT} | |

## Project Structure

{Generate a tree diagram based on the chosen stack. Adapt to the actual frameworks — e.g., NestJS uses src/modules/, Django uses apps/, CLI uses src/commands/. Annotate each directory with its purpose.}

## Application Structure

### Pages / Routes / Screens

{Only for app types with UI. Generate from Phase 3 answers.}

| Page | Path | Description | Auth Required | Roles |
|------|------|-------------|---------------|-------|
| {page} | /{path} | {description} | {yes/no} | {roles} |

### Navigation Pattern

{Navigation structure from Phase 3.}

### Layout

{Layout regions and responsive behavior from Phase 3.}

### API Surface

{For all app types with a backend. Resource-oriented overview.}

| Method | Endpoint | Description | Auth |
|--------|----------|-------------|------|
| {GET/POST/etc} | /api/v1/{resource} | {description} | {yes/no} |

### Key Interactions

{Primary interaction patterns from Phase 3.}

### Data Display Patterns

{How data is shown to users from Phase 3.}

### External Integrations

{Planned integrations from Phase 3.}

## Authentication & Authorization

### Auth Flow
{Step-by-step: user visits → login page → enters credentials → redirected to dashboard}

### Roles & Permissions
| Role | Description | Access Level |
|------|-------------|-------------|
| {role} | {description} | {what they can see/do} |

## State Management Strategy

{Approach based on state complexity from Phase 3.}

## Theming & Responsive

{Device support and theming approach from Phase 3.}

## Notification Strategy

{Communication channels from Phase 3.}

## Key Conventions

- **File naming**: {e.g., kebab-case for files, PascalCase for components}
- **Error handling**: {approach based on stack}
- **API response format**: {e.g., { data, error, meta }}
- **Logging**: {approach}
- **Environment variables**: {.env pattern}

## Scaffolding Commands

{Stack-specific commands to bootstrap the project. Generate based on chosen stack — see stack-catalog.md for examples.}

## Decision Log

| Date | Decision | Rationale |
|------|----------|-----------|
| {today} | Chose {APP_TYPE} application type | {rationale} |
| {today} | Chose {ARCH_PATTERN} architecture | {rationale} |
| {today} | Chose {BACKEND_STACK} + {FRONTEND_STACK} stack | {rationale} |
| {today} | Defined application structure | {summary} |

{Omit sections that don't apply. E.g., for API-only: omit Pages, Navigation, Layout, Theming. For CLI: omit most web-specific sections.}
